<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Cheat Sheet{% endblock %}</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.6.0/css/bootstrap.min.css">
        <link rel="stylesheet" href="{{ asset('/css/style.css') }}" />
        {% block stylesheets %}{% endblock %}
        
        {% block javascripts %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg c2 gradientText f2 font-weight-bold">
            <a class="navbar-brand f4" href="{{path('Home')}}">Cheat Sheet</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link hoverEff" href="{{path('installs')}}">Installs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link hoverEff" href="{{path('setUp')}}">Setup</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link hoverEff" href="{{path('code')}}">Code</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link hoverEff" href="{{path('style')}}">Style</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link hoverEff" href="{{path('bootrap')}}">Bootrap</a>
                    </li>
                </ul>
            </div>
        </nav>
        {% block body %}{% endblock %}
    </body>
</html>
